name: foobar
filename: foo.conf
filters:
  - environment: qa
config:
  host: "{{.name}}_{{.tier}} {{.name}}_{{.tier}}.example.com"
  hostname: "{{.ip}}"
  port: "{{.customPort}}"
  dynamic_forward: foo
  forward_x11: true
